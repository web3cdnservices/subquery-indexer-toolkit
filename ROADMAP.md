- Migrate from `docker-compose` to `docker compose` ( add version check )
- Describe multi-database support. One UI, many PG databases.
- Describe instructions for running slave postgres on backup server.